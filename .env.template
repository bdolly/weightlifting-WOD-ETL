# Environment Variables Template
# Copy this file to .env and fill in the actual values
# DO NOT commit .env to version control

# WordPress API Credentials
# These are used for authenticating with the WordPress REST API
# Can be stored in Secrets Manager (recommended) or as environment variables (fallback)
INVICTUS_USER=your_wordpress_username
INVICTUS_PASS=your_wordpress_password

# SMS Notification Configuration
# Phone number in E.164 format (e.g., +14012560086 for US, +442071234567 for UK)
# Must start with + and include country code
SMS_PHONE_NUMBER=+1234567890

# AWS Configuration
# AWS credentials are typically configured via AWS CLI profile
# Profile name used: serverless-agent
# Configure with: aws configure --profile serverless-agent

# Optional: AWS Region (defaults to us-east-1 in serverless.yml)
# AWS_REGION=us-east-1

# Optional: Serverless Stage (defaults to dev)
# STAGE=dev
